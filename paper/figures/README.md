# `paper/figures/`

Generated by `scripts/make_figures.py` from committed `results/*/metrics.json`
and `results/uq/*.json`. Nothing here is drawn by hand, so a figure can never
disagree with the numbers in the tables.

Naming (these are the **actual generated filenames** — the authoritative list;
`main.tex` is a pre-final 2026-09-03 snapshot and its `\includegraphics` names
may lag). Each is written as both `.pdf` (vector) and `.png` (>=300 dpi):

| File | Figure | Section |
|---|---|---|
| `fig01_schematic` | 1. Three families + shared protocol | Introduction |
| `fig02_cp_profiles` | 2. cp distributions, 4 airfoils, ID + 3 OOD | Results, in-distribution |
| `fig03_surface_fields` | 3. Surface pressure fields, pred/truth/error | Results, in-distribution |
| `fig04_error_vs_shift` | 4. **Error vs shift magnitude** (the central figure) | Results, OOD |
| `fig05_fsc_scatter` | 5. Force self-consistency scatter | Results, consistency |
| `fig06_reliability` | 6. Reliability diagram | Results, calibration |
| `fig07_coverage_vs_shift` | 7. Coverage vs shift magnitude | Results, calibration |
| `fig08_interval_width` | 8. Interval-width distributions | Results, calibration |
| `fig09_data_efficiency` | 9. Data-efficiency curves | Results, efficiency |
| `fig10_symmetry` | 10. Symmetry residual per model | Results, consistency |
| `fig11_active_pool` | 11a. Active-learning pool scoring (selection) | Active learning |
| `fig11_active_verify` | 11b. Surrogate-vs-Fluent by arm (accepted set) | Active learning |
| `fig12_cost_accuracy` | 12. Cost-accuracy Pareto | Results, efficiency |

Everything here is drawn by `scripts/make_figures.py` from the committed
`results/` tree, so a figure can never disagree with the tables (both come from
the same loader, `src/viz/data.load_runs`, with the same tag/split filtering).
